import React from 'react';
import List from './list';

function History({ req }) {
  console.log('history', req);
  return <div id='method'>{/* <List data={props} /> */}</div>;
}

export default History;
